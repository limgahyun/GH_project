<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.project.gh.Threekingdoms.Wei.WeiMapper">
  
  	<!-- 위나라 장수 등록 -->
  	<insert id="insertWeiGeneral" parameterType="com.project.gh.Threekingdoms.Wei.WeiVO">
		INSERT INTO wei_general (
			adult_name
			, name
			, explicate
		) VALUES (
			#{adult_name}
			, #{name}
			, #{explicate}
		)
	</insert>
	
	<!-- 위나라 장수 리스트(위나라 홈화면) -->
	<select id="selectWeiGeneral" resultType="com.project.gh.Threekingdoms.Wei.WeiVO">
		SELECT
			adult_name as adult_name
			, name as name
			, explicate as explicate
		FROM wei_general
	</select>
  
  </mapper>